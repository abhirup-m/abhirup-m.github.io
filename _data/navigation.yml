header:

  - titles:
      # @start locale config
      en      : &EN       Work
      # @end locale config
    url: /work/

  - titles:
      # @start locale config
      en      : &EN       Research Interests
      # @end locale config
    url: /interests/

  - titles:
      # @start locale config
      en      : &EN       Other Stuff
      # @end locale config
    url: /other/

  - titles:
      # @start locale config
      en      : &EN       About Me
      # @end locale config
    url: /about/

  - titles:
      # @start locale config
      en      : &EN       Contact Info
      # @end locale config
    url: /contact/
