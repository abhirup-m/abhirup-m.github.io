header:

  - titles:
      # @start locale config
      en      : &EN       Work
      # @end locale config
    url: /work/

  - titles:
      # @start locale config
      en      : &EN       Research Interests
      # @end locale config
    url: /interests/

  - titles:
      # @start locale config
      en      : &EN       Posts
      # @end locale config
    url: /posts/

  - titles:
      # @start locale config
      en      : &EN       Contact Info
      # @end locale config
    url: /contact/
